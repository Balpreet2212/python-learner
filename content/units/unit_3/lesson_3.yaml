title: "elif Chains"
xp: 12

exercises:
  - type: concept
    code: |
      score = 82
      if score >= 90:
          print("A")
      elif score >= 75:
          print("B")
      elif score >= 60:
          print("C")
      else:
          print("F")
    output: "B"
    explanation: "elif (else if) adds more conditions. Python checks each in order and runs only the first True branch."

  - type: mcq
    question: "What does this print?"
    code: "x = 50\nif x > 100:\n    print(\"big\")\nelif x > 25:\n    print(\"medium\")\nelse:\n    print(\"small\")"
    choices: ["medium", "big", "small", "big\nmedium"]
    correct: "medium"
    explanation: "x > 100 is False. x > 25 is True (50 > 25), so 'medium' prints."

  - type: fill_blank
    prompt: "Add a second speed check that only runs when the first condition was false."
    before: "if speed > 120:\n    print(\"fast\")\n"
    after: " speed > 60:\n    print(\"normal\")\nelse:\n    print(\"slow\")"
    choices: ["elif", "else", "if", "then"]
    answer: "elif"
    explanation: "elif adds another condition to check if the first one was False."

  - type: mcq
    question: "What does this print?"
    code: "temp = 100\nif temp > 100:\n    print(\"boiling\")\nelif temp == 100:\n    print(\"at boiling point\")\nelse:\n    print(\"below\")"
    choices: ["at boiling point", "boiling", "below", "boiling\nat boiling point"]
    correct: "at boiling point"
    explanation: "temp > 100 is False. temp == 100 is True, so the elif branch runs."

  - type: arrange
    instruction: "Build code that prints 'high' when x is greater than 10, 'mid' when x is greater than 5, and 'low' otherwise"
    blocks: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
    correct: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
    explanation: "Python checks conditions in order and runs only the first True branch."

  - type: mcq
    question: "What does this print when score = 90?"
    code: "score = 90\nif score >= 90:\n    print(\"A\")\nelif score >= 90:\n    print(\"also A\")\nelse:\n    print(\"other\")"
    choices: ["A", "also A", "A\nalso A", "other"]
    correct: "A"
    explanation: "The first True branch wins. Even though the elif is also True, only the first matching branch runs."

  - type: fill_blank
    prompt: "Catch all remaining cases where hp is 25 or below, marking them as critical."
    before: "if hp > 75:\n    status = \"healthy\"\nelif hp > 25:\n    status = \"injured\"\n"
    after: ":\n    status = \"critical\""
    choices: ["else", "elif", "if", "then"]
    answer: "else"
    explanation: "else catches all remaining cases — when hp is 25 or below."

  - type: mcq
    question: "What does this print?"
    code: "n = 5\nif n > 5:\n    print(\"A\")\nelif n > 3:\n    print(\"B\")\nelif n > 1:\n    print(\"C\")"
    choices: ["B", "A", "C", "B\nC"]
    correct: "B"
    explanation: "n > 5 is False. n > 3 is True, so 'B' prints. C is not reached."

  - type: mcq
    question: "How many elif branches can you have?"
    code: ""
    choices: ["As many as you need", "Exactly one", "Maximum 2", "None — use nested if instead"]
    correct: "As many as you need"
    explanation: "You can chain as many elif branches as needed, but only the first True one runs."

  - type: mini_code
    prompt: "Read a score with input(). Print 'A' if 90 or above, 'B' if 75 or above, 'C' if 60 or above, else 'F'."
    starter: "score = int(input())\n"
    tests:
      - code: "assert _stdout.strip() == 'C'"
        stdin: "73"
        message: "score 73 is between 60 and 74, grade is C"
      - code: "assert _stdout.strip() == 'A'"
        stdin: "91"
        message: "score 91 is 90 or above, grade is A"
      - code: "assert _stdout.strip() == 'B'"
        stdin: "80"
        message: "score 80 is between 75 and 89, grade is B"
      - code: "assert _stdout.strip() == 'F'"
        stdin: "50"
        message: "score 50 is below 60, grade is F"

  - type: mini_code
    prompt: "Read a BMI value with input(). Print 'underweight' if below 18.5, 'normal' if below 25, 'overweight' if below 30, else 'obese'."
    starter: "bmi = float(input())\n"
    tests:
      - code: "assert _stdout.strip() == 'normal'"
        stdin: "22"
        message: "22 is between 18.5 and 25, prints 'normal'"
      - code: "assert _stdout.strip() == 'underweight'"
        stdin: "17"
        message: "17 is below 18.5, prints 'underweight'"
      - code: "assert _stdout.strip() == 'overweight'"
        stdin: "27"
        message: "27 is between 25 and 30, prints 'overweight'"
      - code: "assert _stdout.strip() == 'obese'"
        stdin: "31"
        message: "31 is 30 or above, prints 'obese'"

worlds:
  fantasy:
    title: "The Rank Crystal"
    exercises:
      - type: concept
        code: |
          threat_level = 82
          if threat_level >= 90:
              print("Legendary beast!")
          elif threat_level >= 75:
              print("Dangerous creature.")
          elif threat_level >= 60:
              print("Minor threat.")
          else:
              print("Harmless critter.")
        output: "Dangerous creature."
        explanation: "Ranger Kira checks threat level through each branch in order. 82 is not >= 90, but it is >= 75, so 'Dangerous creature.' prints."

      - type: mcq
        question: "What does this print?"
        code: "beast_size = 50\nif beast_size > 100:\n    print(\"big\")\nelif beast_size > 25:\n    print(\"medium\")\nelse:\n    print(\"small\")"
        choices: ["medium", "big", "small", "big\nmedium"]
        correct: "medium"
        explanation: "beast_size > 100 is False. beast_size > 25 is True (50 > 25), so 'medium' prints. A medium-sized beast."

      - type: fill_blank
        prompt: "Add a second creature speed check that only runs when the first condition was false."
        before: "if creature_speed > 120:\n    print(\"fast\")\n"
        after: " creature_speed > 60:\n    print(\"normal\")\nelse:\n    print(\"slow\")"
        choices: ["elif", "else", "if", "then"]
        answer: "elif"
        explanation: "elif adds another speed tier to check if the first one was False. Kira knows how to pace her response."

      - type: mcq
        question: "What does this print?"
        code: "dragon_heat = 100\nif dragon_heat > 100:\n    print(\"boiling\")\nelif dragon_heat == 100:\n    print(\"at boiling point\")\nelse:\n    print(\"below\")"
        choices: ["at boiling point", "boiling", "below", "boiling\nat boiling point"]
        correct: "at boiling point"
        explanation: "dragon_heat > 100 is False. dragon_heat == 100 is True, so the elif branch runs — the dragon is at peak heat."

      - type: arrange
        instruction: "Build code that prints 'high' when x is greater than 10, 'mid' when x is greater than 5, and 'low' otherwise"
        blocks: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
        correct: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
        explanation: "Kira ranks the threat from high to low. Python checks each condition in order and runs only the first True branch."

      - type: mcq
        question: "What does this print when score = 90?"
        code: "score = 90\nif score >= 90:\n    print(\"A\")\nelif score >= 90:\n    print(\"also A\")\nelse:\n    print(\"other\")"
        choices: ["A", "also A", "A\nalso A", "other"]
        correct: "A"
        explanation: "The first True branch wins. Even though the elif is also True, only the first matching branch runs — Kira trusts the first reading."

      - type: fill_blank
        prompt: "Catch all remaining cases where Kira's hp is 25 or below, marking her as critically wounded."
        before: "if hp > 75:\n    status = \"healthy\"\nelif hp > 25:\n    status = \"injured\"\n"
        after: ":\n    status = \"critical\""
        choices: ["else", "elif", "if", "then"]
        answer: "else"
        explanation: "else catches all remaining cases — when hp is 25 or below, Kira is in critical condition."

      - type: mcq
        question: "What does this print?"
        code: "n = 5\nif n > 5:\n    print(\"A\")\nelif n > 3:\n    print(\"B\")\nelif n > 1:\n    print(\"C\")"
        choices: ["B", "A", "C", "B\nC"]
        correct: "B"
        explanation: "n > 5 is False. n > 3 is True, so 'B' prints. Kira gets a B-rank on the creature scale."

      - type: mcq
        question: "How many elif branches can you have?"
        code: ""
        choices: ["As many as you need", "Exactly one", "Maximum 2", "None — use nested if instead"]
        correct: "As many as you need"
        explanation: "Kira can add as many creature tiers as she needs — only the first matching tier triggers."

      - type: mini_code
        prompt: "The Rank Crystal glows with a score for each beast Kira encounters. Read a score with input(). Print 'A' if 90 or above, 'B' if 75 or above, 'C' if 60 or above, else 'F'."
        starter: "score = int(input())\n"
        tests:
          - code: "assert _stdout.strip() == 'C'"
            stdin: "73"
            message: "score 73 is between 60 and 74, grade is C"
          - code: "assert _stdout.strip() == 'A'"
            stdin: "91"
            message: "score 91 is 90 or above, grade is A"
          - code: "assert _stdout.strip() == 'B'"
            stdin: "80"
            message: "score 80 is between 75 and 89, grade is B"
          - code: "assert _stdout.strip() == 'F'"
            stdin: "50"
            message: "score 50 is below 60, grade is F"

      - type: mini_code
        prompt: "Kira's bestiary rates each creature by mass index. Read a value with input(). Print 'underweight' if below 18.5, 'normal' if below 25, 'overweight' if below 30, else 'obese'."
        starter: "bmi = float(input())\n"
        tests:
          - code: "assert _stdout.strip() == 'normal'"
            stdin: "22"
            message: "22 is between 18.5 and 25, prints 'normal'"
          - code: "assert _stdout.strip() == 'underweight'"
            stdin: "17"
            message: "17 is below 18.5, prints 'underweight'"
          - code: "assert _stdout.strip() == 'overweight'"
            stdin: "27"
            message: "27 is between 25 and 30, prints 'overweight'"
          - code: "assert _stdout.strip() == 'obese'"
            stdin: "31"
            message: "31 is 30 or above, prints 'obese'"

  scifi:
    title: "Status Levels"
    exercises:
      - type: concept
        code: |
          hull_integrity = 82
          if hull_integrity >= 90:
              print("Hull optimal.")
          elif hull_integrity >= 75:
              print("Hull stable.")
          elif hull_integrity >= 60:
              print("Hull damaged.")
          else:
              print("Hull critical!")
        output: "Hull stable."
        explanation: "Captain Voss runs through the hull status chain. 82 is not >= 90, but it is >= 75, so 'Hull stable.' prints."

      - type: mcq
        question: "What does this print?"
        code: "reactor_output = 50\nif reactor_output > 100:\n    print(\"big\")\nelif reactor_output > 25:\n    print(\"medium\")\nelse:\n    print(\"small\")"
        choices: ["medium", "big", "small", "big\nmedium"]
        correct: "medium"
        explanation: "reactor_output > 100 is False. reactor_output > 25 is True (50 > 25), so 'medium' prints. A medium reactor output."

      - type: fill_blank
        prompt: "Add a second thruster speed check that only runs when the first condition was false."
        before: "if thruster_speed > 120:\n    print(\"fast\")\n"
        after: " thruster_speed > 60:\n    print(\"normal\")\nelse:\n    print(\"slow\")"
        choices: ["elif", "else", "if", "then"]
        answer: "elif"
        explanation: "elif checks the next speed tier if the first was False. Voss classifies thruster output precisely."

      - type: mcq
        question: "What does this print?"
        code: "core_temp = 100\nif core_temp > 100:\n    print(\"boiling\")\nelif core_temp == 100:\n    print(\"at boiling point\")\nelse:\n    print(\"below\")"
        choices: ["at boiling point", "boiling", "below", "boiling\nat boiling point"]
        correct: "at boiling point"
        explanation: "core_temp > 100 is False. core_temp == 100 is True, so the elif branch runs — reactor is at critical temperature."

      - type: arrange
        instruction: "Build code that prints 'high' when x is greater than 10, 'mid' when x is greater than 5, and 'low' otherwise"
        blocks: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
        correct: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
        explanation: "Voss ranks the alert level from high to low. Python checks each condition in order and runs only the first True branch."

      - type: mcq
        question: "What does this print when score = 90?"
        code: "score = 90\nif score >= 90:\n    print(\"A\")\nelif score >= 90:\n    print(\"also A\")\nelse:\n    print(\"other\")"
        choices: ["A", "also A", "A\nalso A", "other"]
        correct: "A"
        explanation: "The first True branch wins. Even though the elif is also True, only the first matching branch runs — the first sensor reading takes priority."

      - type: fill_blank
        prompt: "Catch all remaining cases where hull_integrity is 25 or below, marking the hull as critical."
        before: "if hull_integrity > 75:\n    status = \"healthy\"\nelif hull_integrity > 25:\n    status = \"injured\"\n"
        after: ":\n    status = \"critical\""
        choices: ["else", "elif", "if", "then"]
        answer: "else"
        explanation: "else catches all remaining cases — when hull_integrity is 25 or below, the ship is in critical condition."

      - type: mcq
        question: "What does this print?"
        code: "n = 5\nif n > 5:\n    print(\"A\")\nelif n > 3:\n    print(\"B\")\nelif n > 1:\n    print(\"C\")"
        choices: ["B", "A", "C", "B\nC"]
        correct: "B"
        explanation: "n > 5 is False. n > 3 is True, so 'B' prints. The sector gets a B-level alert rating."

      - type: mcq
        question: "How many elif branches can you have?"
        code: ""
        choices: ["As many as you need", "Exactly one", "Maximum 2", "None — use nested if instead"]
        correct: "As many as you need"
        explanation: "Voss can stack as many alert tiers as the checklist requires — only the first matching tier triggers."

      - type: mini_code
        prompt: "The ship's performance log grades each system check. Read a score with input(). Print 'A' if 90 or above, 'B' if 75 or above, 'C' if 60 or above, else 'F'."
        starter: "score = int(input())\n"
        tests:
          - code: "assert _stdout.strip() == 'C'"
            stdin: "73"
            message: "score 73 is between 60 and 74, grade is C"
          - code: "assert _stdout.strip() == 'A'"
            stdin: "91"
            message: "score 91 is 90 or above, grade is A"
          - code: "assert _stdout.strip() == 'B'"
            stdin: "80"
            message: "score 80 is between 75 and 89, grade is B"
          - code: "assert _stdout.strip() == 'F'"
            stdin: "50"
            message: "score 50 is below 60, grade is F"

      - type: mini_code
        prompt: "The med-bay scanner classifies each crew member by body mass index. Read a value with input(). Print 'underweight' if below 18.5, 'normal' if below 25, 'overweight' if below 30, else 'obese'."
        starter: "bmi = float(input())\n"
        tests:
          - code: "assert _stdout.strip() == 'normal'"
            stdin: "22"
            message: "22 is between 18.5 and 25, prints 'normal'"
          - code: "assert _stdout.strip() == 'underweight'"
            stdin: "17"
            message: "17 is below 18.5, prints 'underweight'"
          - code: "assert _stdout.strip() == 'overweight'"
            stdin: "27"
            message: "27 is between 25 and 30, prints 'overweight'"
          - code: "assert _stdout.strip() == 'obese'"
            stdin: "31"
            message: "31 is 30 or above, prints 'obese'"

  mystery:
    title: "Ranking Suspects"
    exercises:
      - type: concept
        code: |
          suspicion_level = 82
          if suspicion_level >= 90:
              print("Prime suspect!")
          elif suspicion_level >= 75:
              print("Strong lead.")
          elif suspicion_level >= 60:
              print("Worth questioning.")
          else:
              print("Likely innocent.")
        output: "Strong lead."
        explanation: "Detective Cole scores each suspect. 82 is not >= 90, but it is >= 75, so 'Strong lead.' prints — this person needs a closer look."

      - type: mcq
        question: "What does this print?"
        code: "clue_weight = 50\nif clue_weight > 100:\n    print(\"big\")\nelif clue_weight > 25:\n    print(\"medium\")\nelse:\n    print(\"small\")"
        choices: ["medium", "big", "small", "big\nmedium"]
        correct: "medium"
        explanation: "clue_weight > 100 is False. clue_weight > 25 is True (50 > 25), so 'medium' prints. A medium-weight clue."

      - type: fill_blank
        prompt: "Add a second urgency check for the investigation that only runs when the first condition was false."
        before: "if urgency_level > 120:\n    print(\"fast\")\n"
        after: " urgency_level > 60:\n    print(\"normal\")\nelse:\n    print(\"slow\")"
        choices: ["elif", "else", "if", "then"]
        answer: "elif"
        explanation: "elif checks the next urgency tier if the first was False. Cole classifies how quickly to act."

      - type: mcq
        question: "What does this print?"
        code: "tension_level = 100\nif tension_level > 100:\n    print(\"boiling\")\nelif tension_level == 100:\n    print(\"at boiling point\")\nelse:\n    print(\"below\")"
        choices: ["at boiling point", "boiling", "below", "boiling\nat boiling point"]
        correct: "at boiling point"
        explanation: "tension_level > 100 is False. tension_level == 100 is True, so the elif branch runs — the interrogation is at peak tension."

      - type: arrange
        instruction: "Build code that prints 'high' when x is greater than 10, 'mid' when x is greater than 5, and 'low' otherwise"
        blocks: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
        correct: ["if", "x", ">", "10", ":", "print", "(", "\"high\"", ")", "elif", "x", ">", "5", ":", "print", "(", "\"mid\"", ")", "else", ":", "print", "(", "\"low\"", ")"]
        explanation: "Cole ranks the threat level from high to low. Python checks each condition in order and runs only the first True branch."

      - type: mcq
        question: "What does this print when score = 90?"
        code: "score = 90\nif score >= 90:\n    print(\"A\")\nelif score >= 90:\n    print(\"also A\")\nelse:\n    print(\"other\")"
        choices: ["A", "also A", "A\nalso A", "other"]
        correct: "A"
        explanation: "The first True branch wins. Even though the elif is also True, only the first matching branch runs — Cole acts on the first solid lead."

      - type: fill_blank
        prompt: "Catch all remaining cases where alibi_strength is 25 or below, marking the suspect as critically suspicious."
        before: "if alibi_strength > 75:\n    status = \"healthy\"\nelif alibi_strength > 25:\n    status = \"injured\"\n"
        after: ":\n    status = \"critical\""
        choices: ["else", "elif", "if", "then"]
        answer: "else"
        explanation: "else catches all remaining cases — when alibi_strength is 25 or below, the suspect is critically suspicious."

      - type: mcq
        question: "What does this print?"
        code: "n = 5\nif n > 5:\n    print(\"A\")\nelif n > 3:\n    print(\"B\")\nelif n > 1:\n    print(\"C\")"
        choices: ["B", "A", "C", "B\nC"]
        correct: "B"
        explanation: "n > 5 is False. n > 3 is True, so 'B' prints. This clue gets a B-tier importance rating."

      - type: mcq
        question: "How many elif branches can you have?"
        code: ""
        choices: ["As many as you need", "Exactly one", "Maximum 2", "None — use nested if instead"]
        correct: "As many as you need"
        explanation: "Cole can add as many suspect tiers as the case requires — only the first matching tier triggers."

      - type: mini_code
        prompt: "Cole's case file grades each piece of evidence. Read a score with input(). Print 'A' if 90 or above, 'B' if 75 or above, 'C' if 60 or above, else 'F'."
        starter: "score = int(input())\n"
        tests:
          - code: "assert _stdout.strip() == 'C'"
            stdin: "73"
            message: "score 73 is between 60 and 74, grade is C"
          - code: "assert _stdout.strip() == 'A'"
            stdin: "91"
            message: "score 91 is 90 or above, grade is A"
          - code: "assert _stdout.strip() == 'B'"
            stdin: "80"
            message: "score 80 is between 75 and 89, grade is B"
          - code: "assert _stdout.strip() == 'F'"
            stdin: "50"
            message: "score 50 is below 60, grade is F"

      - type: mini_code
        prompt: "Cole's forensic scanner classifies physical evidence by density index. Read a value with input(). Print 'underweight' if below 18.5, 'normal' if below 25, 'overweight' if below 30, else 'obese'."
        starter: "bmi = float(input())\n"
        tests:
          - code: "assert _stdout.strip() == 'normal'"
            stdin: "22"
            message: "22 is between 18.5 and 25, prints 'normal'"
          - code: "assert _stdout.strip() == 'underweight'"
            stdin: "17"
            message: "17 is below 18.5, prints 'underweight'"
          - code: "assert _stdout.strip() == 'overweight'"
            stdin: "27"
            message: "27 is between 25 and 30, prints 'overweight'"
          - code: "assert _stdout.strip() == 'obese'"
            stdin: "31"
            message: "31 is 30 or above, prints 'obese'"
